Task 4

建模： 对于本课题的数据，可以用多种算法（模型）包括basline里用LightGBM模型、还有logistic回归，knn近邻法、bayesian方法等。
如何选择最适合的模型：都尝试一遍并且变换模型里的各个参数，看看效果，如无给定的判定指标，用以下指标判定模型的好坏：
精准率/查准率=（TP）/(TP+FP)
召回率/查全率=TP/(TP+FN)
不能单纯追求某个指标的高低，需要考虑具体的情况，追求两者的平衡（ROC曲线或AUC等）

后续需要对算法详细了解和程序实现。 
